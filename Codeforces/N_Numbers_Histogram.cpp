#include<bits/stdc++.h>
using namespace std;

int main()
{
    char s;
    int n,x;

    cin >> s >> n;

    while(n--)
    {
        cin >> x;
        while(x--)
        {
            cout << s;
        }
        cout << endl;
    }
}